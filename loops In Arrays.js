let marks = [1, 3, 4, 5, 7, 8, 0];
for (i = 0; i < marks.length; i++) {
  console.log(marks[i]);
}

// For each loop
marks.forEach((ofStudents) => { // index,value ,array
  console.log(ofStudents - 2); // it runs every value of arrays
});

//Array.from is used to convert the html contents into objects using for each()loop


// map method 
// it creates a new array for perfoming operations on each array element

